
def build(bld):

    bld(features = 'cxx cxxprogram',
        target = 'simple_encrypt_ctr_example',
        source = 'simple_encrypt_ctr_example.cpp',
        lib = ['crypto'],        
        use = ['harpocrates_includes', 'harpocrates'])    
